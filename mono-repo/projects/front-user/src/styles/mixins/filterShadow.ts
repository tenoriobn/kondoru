import { css } from 'styled-components';

export const filterXL = css`
  filter: drop-shadow(6px 6px 4px rgba(0,0,0,0.39));
  -webkit-filter: drop-shadow(6px 6px 4px rgba(0,0,0,0.39));
  -moz-filter: drop-shadow(6px 6px 4px rgba(0,0,0,0.39));
`;
